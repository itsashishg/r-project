import { EventDirectiveDirective } from './event-directive.directive';

describe('EventDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EventDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
